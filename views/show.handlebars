
<h1 class="mb-1 restaurant-show-title">{{rstrt.name}}</h1>
<div class="container">
  <div class="row">
    <div class="col-12 col-md-10 mx-auto">

      <p class="mb-1">
        <span class="text-secondary">
          <i class="fas fa-utensils pr-2"></i>
          類別：
        </span>
        {{rstrt.category}}
      </p>

      <p class="mb-1">
        <span class="text-secondary">
          <i class="fas fa-map-marker-alt pr-2"></i>
          地址：
        </span>
        {{rstrt.location}}
        <a href={{rstrt.google_map}} class="text-secondary" target="_blank">
          <i class="fas fa-location-arrow pr-2 fa-xs"></i>
        </a>
      </p>

      <p class="mb-1">
        <span class="text-secondary">
          <i class="fas fa-mobile-alt pr-2"></i>
          電話：
        </span>
        {{rstrt.phone}}
      </p>

      <p class="mb-5">
        {{rstrt.description}}
      </p>

      <img class="rounded mx-auto d-block mb-4 w-100"
        src={{rstrt.image}} alt="{{rstrt.name}}" onerror="this.src='https://i.ytimg.com/vi/pCXb4bgoByU/maxresdefault.jpg'"
        style="max-width: 600px;">
      <nav class="d-flex my-2">
        <a href="/restaurants/{{rstrt._id}}/edit" class="btn btn-primary btn-lg mx-auto">編輯資訊</a>
        <button type="button" class="btn btn-dark text-white btn-lg mx-auto" data-toggle="modal"
          data-target="#deleteConfirmModal-{{rstrt._id}}">刪除</button>
      </nav>
    </div>
  </div>
</div>

<!-- delete confirmation modal -->
<div class="modal fade" id="deleteConfirmModal-{{rstrt._id}}" tabindex="-1" role="dialog"
  aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">確定要刪除這間餐廳嗎?</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        即將刪除 {{rstrt.name}}。
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-primary" data-dismiss="modal">心中仍然不捨🤤</button>
        <form action="/restaurants/{{rstrt._id}}?_method=DELETE" method="POST">
          <button type="submit" class="btn border border-dark">刪了它</button>
        </form>
      </div>
    </div>
  </div>
</div>